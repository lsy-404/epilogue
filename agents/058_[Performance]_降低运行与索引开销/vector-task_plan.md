# 向量存储性能子任务计划

- [x] 阅读项目约束、现有实现和测试，定位同步读取、检索排序和纯元数据写入开销。
- [x] 为二进制扫描减少单条记录的同步读取次数，并保持截断文件安全停止。
- [x] 为向量检索实现固定容量 top-k，避免候选全量排序。
- [x] 区分元数据与向量变更，避免元数据更新重写 vectors.bin。
- [x] 添加行为及系统调用计数回归测试，运行基线对比和完整测试。
- [x] 审阅差异并提交限定范围的改动。
- [x] 定位逐记录同步读取仍造成 25k 库主进程阻塞的热点。
- [x] 以固定大小顺序缓冲扫描 EVB1，并解析跨块记录而不整文件载入。
- [x] 覆盖跨块最大维度、长 ID、短读取与截断的回归场景。
- [x] 用临时旧实现测量 25k×384 查询的耗时与 readSync 次数，提交补充优化。

- [x] 区分缺失 vector 与显式 vector:null，删除过期向量并保证检索立即排除。
- [x] 固定既有记录的内部 ID，防止 idIndex 留下过期映射；添加回归并提交。
