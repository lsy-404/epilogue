# 模型进程驻留开销优化计划

- [x] 阅读项目约束并审查当前模型宿主生命周期。
- [x] 找出关闭窗口、并发请求与子进程退出之间的竞态。
- [x] 实现空闲回收与安全的子进程世代隔离。
- [x] 编写根目录测试覆盖空闲回收和繁忙保护。
- [x] 执行相关测试并提交独立分支变更。


## 设置变更重启收敛

- [x] 审查 `applyAppSettings` 的调用来源和批量索引请求间隔。
- [x] 仅在模型子进程环境值变化时重启。
- [x] 为设置变更和批量请求间隔添加回归测试。
- [x] 运行验证并提交补充变更。
## Store 恢复接线与复审

- [x] 复审主仓 IPC 的 store 引用计数和延迟卸载路径。
- [x] 在开窗路径恢复 store，取消待执行卸载。
- [x] 验证并提交独立接线变更。
## 回收持久性与投递失败

- [x] 确认首次回收后托盘意图被错误清除，且同步投递失败会遗留 pending。
- [x] 保留托盘回收意图并清理同步投递失败的 pending。
- [x] 添加两项 fake-timer/throw 回归测试。
- [x] 验证并提交修复。

## 旧宿主退出后的空闲回收

- [x] 确认旧 pending 阻止新宿主完成时安排回收的事件排序。
- [x] 在旧宿主 exit 清理后重新安排回收。
- [x] 添加此顺序的 fake-timer 回归测试并提交。
