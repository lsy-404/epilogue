# 性能和开销优化计划

- [x] 读取项目审计和实现，确认 main 工作区干净。
- [ ] 建立隔离 worktree，分派向量存储、文件扫描和模型生命周期优化。
- [ ] 主 agent 检查 IPC 和渲染层开销，完成适合当前架构的优化。
- [ ] 运行基线和针对性测试，测量有代表性的性能变化。
- [ ] 审阅子 agent 提交并合并到本地 main。
- [ ] 完成全套验证、检查远程 main 保护状态并按授权交付。

## 主进程与列表优化
- [ ] 将库查询和日期范围计算留在主进程，IPC 每次仅返回一页，更多按钮按页追加。
- [ ] 删除 renderer 全量 libraryCache，守护筛选异步竞态。
- [ ] 最近记录用有限候选取代全量排序。
- [ ] 添加分页边界、筛选、数据量和竞态相关验证。

## 托盘数据生命周期修复
- [ ] withStore 引用计数统一保护跨 await 的库操作，关窗后最后使用者结束再卸载。
- [ ] 覆盖索引、搜索、移动撤销、助手状态和后台 scheduler 路径；开窗取消卸载意图。
- [ ] 测试交错关窗/重开/写入保留与后台结束后释放。
