# 扫描与索引优化计划

- [x] 阅读项目约束、扫描和索引实现，确定不改变可见行为的热点。
- [x] 编写回归测试，覆盖生成器的惰性枚举和递归过滤。
- [x] 避免索引先累积全量路径；重叠根保留现有的逐根索引语义，以免跳过按根目录内容权限触发的全文升级。
- [x] 运行测试和可重复测量，记录结果。
- [x] 检查差异并提交独立分支。
- [x] 让 scheduler 的后台索引和 Solo 流程通过 `withStore` 持有并释放索引库。
- [x] 验证 scheduler 仍保持同步 tick API，并提交独立变更。
